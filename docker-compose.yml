version: '3.1'
services:
  app:
    image: ip-challenge-meli
    expose:
      - '8090'
    deploy:
      replicas: 3
  loadbalancer:
    image: 'dockercloud/haproxy:latest'
    links:
      - app
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - '80:80'